name: Test Bluesky Formatting

on:
  workflow_call:
    inputs:
      date:
        description: 'Specific date to test (YYYY-MM-DD format, optional)'
        required: false
        type: string
  workflow_dispatch:
    inputs:
      date:
        description: 'Specific date to test (YYYY-MM-DD format, optional)'
        required: false
        type: string

jobs:
  test-bluesky:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Test Bluesky formatting (no posting)
      env:
        ALBUM_API_KEY: ${{ secrets.ALBUM_API_KEY }}
        ALBUM_FEED_URL: ${{ secrets.ALBUM_FEED_URL }}
        TARGET_DATE: ${{ inputs.date }}
      run: npm run test:bluesky